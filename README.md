# Base Nest API

nest api with some defaults I commonly use.

Typically used within turborepo, there are some "trace artifacts" within `.gitignore` and `Dockerfile` that can easily be deleted out if you don't intend to do so.
